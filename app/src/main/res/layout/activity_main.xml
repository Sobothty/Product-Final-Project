<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- Fragment container that previews HomeFragment at design time -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/layoutFragments"
        android:name="com.bothty.product_finalproject.fragment."
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- BottomNavigationView with menu -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        app:menu="@menu/activity_main_bottom_navigation_view"
        app:labelVisibilityMode="unlabeled"
        android:background="?android:attr/windowBackground"/>
</LinearLayout>
